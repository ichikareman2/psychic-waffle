import { SimpleThemeDirective } from './simple-theme.directive';

describe('SimpleThemeDirective', () => {
  it('should create an instance', () => {
    const directive = new SimpleThemeDirective();
    expect(directive).toBeTruthy();
  });
});
